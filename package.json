{
  "name": "@influpert/agent",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "scripts": {
    "check": "bun run format:check && bun run lint && bun run typecheck && bun run shellcheck && bun test",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "lint": "biome lint .",
    "typecheck": "tsc -p tsconfig.json",
    "shellcheck": "shellcheck agent-entrypoint.sh init-firewall.sh smoke.sh lib/prompt.sh claude/agent-claude.sh",
    "test": "bun test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.11",
    "@types/bun": "^1.4.0",
    "typescript": "^7.0.2"
  }
}
